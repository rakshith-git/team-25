export interface VetDto {
        // When we pass the vetName the clinicName and speciality must be fetched
    vetId?: String;
    clinicName ?:String;
    vetName?: String;
    speciality ?: String;
}